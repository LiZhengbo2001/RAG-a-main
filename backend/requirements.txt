# ── Web 框架 ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.1.3
python-multipart==0.0.18

# ── 数据库 ──
sqlalchemy==2.0.36
aiosqlite==0.20.0
aiomysql==0.3.2

# ── 向量库 ──
pymilvus==2.4.5
# pymilvus 依赖链兼容：environs<=9.5.0, marshmallow>=3,<4

# ── LLM 客户端 ──
openai>=1.58.1
httpx>=0.28.1

# ── 重排序模型 ──
sentence-transformers==3.3.1
torch>=2.4

# ── 文档解析 ──
pypdf==5.1.0
python-docx==1.1.2
markdown==3.7

# ── 多模态（PDF 图片提取 + 表格识别） ──
# unstructured 是可选依赖，需要 Microsoft C++ Build Tools 才能编译
# 装不上时 PDF 解析会自动降级为 pypdf，不影响核心功能
# 如需安装：先安装 VS Build Tools https://visualstudio.microsoft.com/visual-cpp-build-tools/
# pdf2image==1.17.0
# unstructured[pdf]>=0.16,<0.20
pillow>=10.0

# ── 中文分词 ──
jieba==0.42.1

# ── 认证 ──
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# ── 工具 ──
pydantic>=2.10.3
python-dotenv==1.0.1
tiktoken==0.8.0
setuptools<70
marshmallow>=3.13,<4

# ── LangChain 生态 ──
langchain==1.3.4
langgraph==1.2.4
langchain-ollama==1.1.0
langchain-openai
duckduckgo-search==8.1.1
